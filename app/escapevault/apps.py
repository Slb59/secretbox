"""Configuration for the escapevault application."""

from django.apps import AppConfig


class EscapevaultConfig(AppConfig):
    """App configuration for escapevault."""

    default_auto_field = "django.db.models.BigAutoField"
    name = "escapevault"
